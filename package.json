{
  "name": "astem-monorepo",
  "private": true,
  "scripts": {
    "install-all": "npm install --prefix server && npm install --prefix client && npm install",
    "dev": "concurrently \"npm run dev --prefix server\" \"npm run dev --prefix client\"",
    "share": "concurrently \"npm run dev\" \"npx --yes ngrok http 5173 --log=stdout\" \"node scripts/get-ngrok-url.js\""
  },
  "devDependencies": {
    "concurrently": "^8.2.0"
  }
}
